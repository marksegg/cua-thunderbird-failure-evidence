# Stress-Test Report: Task 69b8c4222cb4edbf3c9a6bfb (Thunderbird Genome Study)
## Claim: "Genuine model failure -- filter created under wrong account"
### Date: 2026-04-06 20:45:00

---

## VERDICT: GENUINE MODEL FAILURE. The task is NOT unfair.

The model made an active, deliberate, and wrong decision. The instruction is unambiguous. Below is the full evidence chain.

---

## 1. What the Verifier Expects

**File checked:** `/home/docker/.thunderbird/t5q2a5hp.default-release/Mail/Local Folders/msgFilterRules.dat`

**Expected filter rule:**
```json
{
  "enabled": "yes",
  "action": "Move to folder",
  "actionValue": "mailbox://nobody@Local%20Folders/Research",
  "condition": ["AND (body,contains,genome study)"]
}
```

The verifier reads `Local Folders/msgFilterRules.dat`, parses each filter block (name=, enabled=, type=, action=, actionValue=, condition=), and checks whether any parsed filter matches ALL fields in the expected rule via exact equality (`_match_record`).

**What the verifier actually got:** A 25-byte file containing only:
```
version="9"
logging="no"
```
This is the default empty header -- zero filter rules were written to this file.

---

## 2. What the Model Actually Created (Run 1 Trajectory, 38 steps)

### Steps 1-20: Email handling (CORRECT)
- Found the email from contact@partnermagazine.com with subject "Reviewer Invitation: Genome Study"
- Created "Research" folder under Local Folders
- Moved the email to Local Folders/Research
- **Metric `file_contains` passed (score 1.0)** -- email move was done correctly

### Steps 21-38: Filter creation (THE ERROR)

**Step 24 (the decision point):** The model opened Tools > Message Filters. The dialog opened with "Filters for: Local Folders" selected by default. The model's reasoning was:

> "I need to change the 'Filters for' dropdown to the correct account - I should set it for the email account `dr.carter@genomictechinnovations.com` since that's where incoming emails arrive."

**Step 25:** The model saw the dropdown with two options: `dr.carter@genomictechinnovations.com` and `Local Folders`. It reasoned:

> "I see the dropdown with two options... I need to select the email account since incoming mail filters should be applied there."

**Step 26:** After switching to `dr.carter@genomictechinnovations.com`, the model clicked "New..."

**Steps 27-37:** The model created the filter with:
- **Name:** "Genome Study to Research"
- **Condition:** Body contains "genome study"
- **Action:** Move Message to "Research on Local Folders"

**Step 38:** The model confirmed the filter was saved under `dr.carter@genomictechinnovations.com`.

### Key observation:
The filter's action target was correct (Local Folders/Research). The filter conditions were correct (body contains genome study). The filter was enabled. But it was stored under the IMAP account instead of Local Folders. This means the filter was written to:
`/home/docker/.thunderbird/t5q2a5hp.default-release/Mail/imap.*/msgFilterRules.dat`
instead of:
`/home/docker/.thunderbird/t5q2a5hp.default-release/Mail/Local Folders/msgFilterRules.dat`

---

## 3. Is the Instruction Ambiguous? NO.

The full instruction:
> "Among your Inbox emails, find the email from 'contact@partnermagazine.com' that mentions an invitation to review a genome study. Read the email to confirm it's about a genome study, then move it to 'Local Folders/Research'. **Additionally, create a filter to automatically move future invitations mentioning 'genome study' to this folder.**"

### Why "Local Folders" is the correct account for the filter:

1. **"to this folder"** -- The instruction says move future emails "to this folder" (Local Folders/Research). In Thunderbird, the filter's target folder `mailbox://nobody@Local%20Folders/Research` is under Local Folders. When you want a filter that moves mail TO Local Folders, the most natural place to attach the filter is Local Folders itself, since Local Folders is a local-only store.

2. **The instruction does NOT mention the IMAP account at all.** It never says "filter incoming mail for dr.carter@genomictechinnovations.com." Compare with task `69bb15eeb46c7fadbf00689e` which explicitly says "create and enable a new message filter **for the rachel.nguyen@cbhlaw.com account**" -- that task correctly checks the IMAP path.

3. **The subproblems (gold decomposition) say:**
   - Subproblem 4: "Open Tools > Message Filters > New." (no account switch mentioned)
   - Subproblem 5: "Create a filter: Body contains 'genome study', Move to Research."
   - Subproblem 6: "Ensure the filter is enabled and save."
   
   Nowhere in the gold subproblems does it say to change the "Filters for" dropdown to the IMAP account. The default was already "Local Folders."

4. **Cross-task evidence:** Among 22 Thunderbird filter tasks in this benchmark:
   - ~15 check `Local Folders/msgFilterRules.dat`
   - 2 check IMAP-specific paths (and those tasks explicitly name the IMAP account in their instructions)
   - This is consistent: when the instruction doesn't specify an account, the gold answer is Local Folders.

---

## 4. The Model's Reasoning Was Plausible But Wrong

The model's logic -- "incoming mail filters should be applied to the IMAP account since that's where emails arrive" -- is a reasonable heuristic for a real-world Thunderbird user. In practice:

- Thunderbird filters CAN be attached to either the IMAP account or Local Folders
- IMAP account filters run on incoming mail for that account
- Local Folders filters also work when configured to run on incoming mail

However, the task instruction doesn't ask the model to reason about mail delivery architecture. It asks to "create a filter to automatically move future invitations mentioning 'genome study' to this folder [Local Folders/Research]." The simplest reading: create the filter where the target folder lives, which is Local Folders. The dialog ALREADY defaulted to Local Folders -- the model went out of its way to change it.

---

## 5. Consistency Across Runs

| Run | file_contains (email move) | check_thunderbird_filter | Overall | msgFilterRules.dat size |
|-----|---------------------------|--------------------------|---------|------------------------|
| 1   | PASS (1.0)                | FAIL (0.0)               | 0.0     | 25 bytes (empty)       |
| 2   | PASS (1.0)                | FAIL (0.0)               | 0.0     | 25 bytes (empty)       |
| 3   | PASS (1.0)                | FAIL (0.0)               | 0.0     | 25 bytes (empty)       |
| 4   | (no data)                 | (no data)                | --      | --                     |
| 5   | (no data)                 | (no data)                | --      | --                     |

All three completed runs show the EXACT same failure pattern. The model consistently:
1. Correctly moves the email (pass)
2. Switches the filter account to IMAP instead of leaving it on Local Folders (fail)

Run 2 trajectory confirms identical reasoning: "I need to change the 'Filters for:' dropdown to the correct account (dr.carter@genomictechinnovations.com) since we want to filter incoming emails to the inbox."

Run 3 trajectory confirms identical reasoning: "I see it's showing 'Filters for: Local Folders' with 0 items. I need to change this to filter for the email account (dr.carter@genomictechinnovations.com) since we want to filter incoming emails."

---

## 6. Devil's Advocate Arguments and Rebuttals

### Argument: "The model's choice is technically functional -- an IMAP filter can move mail to Local Folders/Research too."
**Rebuttal:** Functionally maybe, but the verifier checks a specific file. The task has a single correct answer path. More importantly, the model CHANGED the default setting away from what was correct. If both were equally valid, the verifier would need to check both paths -- but the task design clearly intends Local Folders.

### Argument: "The instruction is ambiguous because it doesn't say WHERE to create the filter."
**Rebuttal:** The instruction says "create a filter to... move... to this folder [Local Folders/Research]." The most natural interpretation is to create the filter under Local Folders. The subproblems confirm this by NOT mentioning any account switch. Tasks that want the IMAP account explicitly say so. The dialog defaulted to Local Folders -- the model had to actively override it.

### Argument: "Real users would put the filter on the IMAP account."
**Rebuttal:** This might be a debatable UX preference, but it doesn't matter for task evaluation. The instruction's most direct reading, the subproblem decomposition, and the verifier all agree: Local Folders. The model's "real world" reasoning led it astray from what the task actually asked.

---

## 7. Final Assessment

**Classification: GENUINE MODEL FAILURE**

**Failure mode:** The model applied real-world email reasoning ("filters should go on the account that receives mail") instead of following the simplest interpretation of the instruction. It actively changed the "Filters for" dropdown AWAY from the correct default (Local Folders) to the IMAP account. This is NOT a verifier bug, NOT task ambiguity, and NOT a setup issue.

**Severity:** The model did everything else correctly (found the email, confirmed genome study content, created Research folder, moved the email, configured correct filter conditions and action). The ONLY error was the account selection for filter storage.

**Reproducibility:** 3/3 completed runs show the same failure. The model's reasoning is consistent and deterministic on this point.

**Is this fair?** Yes. The instruction is clear enough that a careful reader would leave the default (Local Folders) alone. The model over-engineered its approach.
